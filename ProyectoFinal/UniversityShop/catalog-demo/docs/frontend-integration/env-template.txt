# .env.local — plantilla para el frontend (Vite + React)
# Crea este archivo en la RAÍZ del proyecto frontend (donde está package.json).
# NO commitear este archivo (.gitignore debe excluirlo).

# URL base de la API del backend — sin trailing slash, CON /api/v1
VITE_API_BASE_URL=http://localhost:8080/api/v1

# ── Para producción ────────────────────────────────────────────────
# Crea un .env.production o configura las variables en tu plataforma de despliegue:
# VITE_API_BASE_URL=https://api.tu-dominio.com/api/v1
